import React, { useState, createRef } from "react";
import Editor from "../editor";

const RightMenu = () => {
	return (
		<div className="left">
			<h3>Form Item</h3>
			{/* <Editor createNewFormItem={(val: keyof IDictionaryType) => {}} /> */}
		</div>
	);
};
