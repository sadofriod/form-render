/** @format */

import React from "react";

interface Props extends BaseComponent {}

const Field: React.SFC<Props> = (props) => {
  const { model, value, absolutePath } = props;
  return <></>;
};

export default Field;
